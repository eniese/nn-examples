# Sample Neural Network code

This project contains some simple python3 code to train and run
a very simple Neural Network that can classify as animal based
on the input (number of legs). It knows two kinds of animals:
dogs & spiders.

train_nn.py -> trains this NN and gives weights & biases
nn.py -> implementation of this simple NN

